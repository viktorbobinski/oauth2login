
server:
  port: ${PORT}

spring:
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: ${google-client-id}
            client-secret: ${google-client-secret}
  datasource:
    url:
    username:
    password: